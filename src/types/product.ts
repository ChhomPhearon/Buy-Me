export type ProductType = {
    id?: string | number,
    name: string,
    image?: string,
    price?: number,
    desc?: string,
    category?: string,
    stock?: number
    products:string;
    map:string;
}